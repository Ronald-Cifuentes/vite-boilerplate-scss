{
  "name": "vite-boilerplate-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated Playwright e2e harness. Kept separate from the locked root package.json so the root app manifest stays unmodified while e2e tests can resolve @playwright/test.",
  "scripts": {
    "test": "playwright test --config playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
